server:
  port: 9300
spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      profile: db
      discovery:
        service-id: config-server
        enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

