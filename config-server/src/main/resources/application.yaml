server:
  port: 9200
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          search-paths: '{application}'
          uri: https://github.com/yanhuasanyue1015/spring-cloud.git
      label: master
